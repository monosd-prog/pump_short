## Backlog

| ID | Epic                  | Task                                                                 | Priority | Status   | Owner Agent             | Acceptance Criteria                                                                                          | Notes |
|----|-----------------------|----------------------------------------------------------------------|----------|----------|-------------------------|-------------------------------------------------------------------------------------------------------------|-------|
| B1 | Signal Quality        | Формализовать контракт входящего сигнала `/pump` (schema + валидация) | High     | Todo     | Strategy Developer Agent | Схема описана в docs; в коде есть строгая валидация; невалидные сигналы логируются с причиной отказа.       | База для качества сигналов и ML. |
| B2 | Signal Quality        | Добавить smoke-тесты для примеров сигналов (валидный/невалидный)     | High     | Todo     | QA / Validation Agent   | Набор автотестов падает при изменении контракта; покрыты типичные edge-cases (symbol, ts, pump_pct).       | Можно использовать существующие smoke-скрипты как шаблон. |
| B3 | Signal Quality        | Ввести метрики принятия/отклонения сигналов                          | Medium   | Todo     | Strategy Developer Agent | Есть агрегаты accepted/rejected по причинам; отчеты доступны через analytics или Telegram-репорты.          | Метрики пригодятся для ML readiness. |
| B4 | Risk Guard            | Задокументировать текущие правила риск-контроля                      | High     | Todo     | Project Manager Agent   | В docs есть раздел с полным описанием risk-политики (size, leverage, max concurrent, cooldown, caps).       | Опорный документ для всех изменений. |
| B5 | Risk Guard            | Добавить unit-тесты на ключевые risk-правила                         | High     | Todo     | QA / Validation Agent   | Тесты падают при нарушении лимитов (size, leverage, max concurrent, cooldown); интегрированы в CI/smoke.     | Покрыть минимум 3–4 типичных сценария. |
| B6 | Risk Guard            | Настроить sanity-check перед деплоем (ENV и конфиги)                 | High     | Todo     | Production Safety Agent | Есть скрипт/команда, которая валидирует критичные ENV/конфиги; деплой без успешной проверки запрещен.       | Может интегрироваться в `deploy_all.sh`. |
| B7 | Outcome Tracking      | Проверить целостность пайплайна `events_v3/trades_v3/outcomes_v3`    | High     | Todo     | QA / Validation Agent   | Для выборочного периода нет необъяснимых дыр и дубликатов; связи event↔trade↔outcome консистентны.          | Использовать существующие диагностики в `docs/DIAG_*.md` как референс. |
| B8 | Outcome Tracking      | Ввести регулярный smoke по outcome-трекеру                           | Medium   | Todo     | QA / Validation Agent   | Smoke-сценарий, который гарантирует появление исхода в течение ожидаемого окна для тестового кейса.        | Кандидат для systemd-timer. |
| B9 | Outcome Tracking      | Добавить алерт при отсутствии новых outcomes дольше SLA              | Medium   | Todo     | Production Safety Agent | При простое outcome-трекера срабатывает алерт в TG; время SLA документировано.                             | Можно строить на уже существующих TG-нотификациях. |
| B10| Dataset / Labeling    | Описать dataset schema и target-лейблы для ML                        | High     | Todo     | Strategy Developer Agent | Есть документ/раздел с перечислением всех полей, таргетов и их семантики; согласован с `dataset_schema.py`. | Базис для ML Readiness. |
| B11| Dataset / Labeling    | Добавить data-quality проверки (NaN, дубликаты, диапазоны значений)  | High     | Todo     | QA / Validation Agent   | Автопроверки находят и репортят аномалии; выполняются для свежих дат перед анализом/ML-экспериментами.      | Можно оформить как отдельный analytics-скрипт. |
| B12| Dataset / Labeling    | Ввести версионирование датасета и схемы                              | Medium   | Todo     | Project Manager Agent   | Определена политика версий (v3+), есть док, как мигрировать и как фиксировать изменения схем.               | Уменьшает риск поломок при изменении формата. |
| B13| Validation / Backtest | Описать стандартный validation playbook для новой гипотезы           | High     | Todo     | Project Manager Agent   | Документирован пошаговый сценарий: выбор дат, команды, метрики, чеклист; используется при новых изменениях. | Можно разместить в `WORKFLOW.md` или отдельном doc. |
| B14| Validation / Backtest | Подготовить минимальный offline-набор для быстрых проверок           | Medium   | Todo     | Strategy Developer Agent | Существует небольшой, но репрезентативный датасет; используется в unit/smoke/ML-экспериментах локально.     | Идеален для CI и локальной отладки. |
| B15| Validation / Backtest | Согласовать базовый набор метрик и порогов (WR, EV, MDD, sample size)| Medium   | Todo     | Project Manager Agent   | Есть таблица целевых метрик с минимальными требованиями; все новые фичи сверяются с этими порогами.         | Ложится в Definition of Done и roadmap. |
| B16| Monitoring / Alerts   | Карта ключевых инцидентов и алертов (what/when/how)                  | Medium   | Todo     | Production Safety Agent | Список инцидентов и правил алертов задокументирован; для каждого инцидента есть понятный owner и реакция.   | Можно основываться на существующих DIAG-доках. |
| B17| Monitoring / Alerts   | Настроить алерты по аномальной просадке и всплеску сделок            | Medium   | Todo     | Production Safety Agent | В TG приходят уведомления при выходе за порог MDD или резком росте количества сделок; пороги задокументированы. | Триггер из аналитики или runtime. |
| B18| Monitoring / Alerts   | Добавить health-checkи для ключевых сервисов                         | High     | Todo     | Production Safety Agent | Есть проверка статуса основных сервисов (watcher, runner, outcome worker); падает при остановке сервиса.    | Можно реализовать как отдельный скрипт + systemd timer. |
| B19| ML Readiness          | Подготовить ML-экспорт датасета (табличный формат с фичами/таргетами)| High     | Todo     | Strategy Developer Agent | Есть стабильный экспорт (CSV/Parquet) с полным набором фич и таргетами; документация по использованию.      | Используется для первых ML-экспериментов. |
| B20| ML Readiness          | Реализовать и задокументировать ML-бейзлайн поверх текущих фильтров  | Medium   | Todo     | Strategy Developer Agent | Есть простой ML-модельный фильтр (baseline), воспроизводимый скрипт обучения и отчет с результатами.        | Нужна четкая валидация и сравнение с rule-based. |
| B21| ML Readiness          | Определить процесс деплоя ML-фильтра в прод (gating + safety)       | Medium   | Todo     | Production Safety Agent | Есть документированный план включения ML в решение о входе (с фолбеком на rule-based и явными границами).   | Важно минимизировать production risk. |
| B22| Monitoring / Alerts   | Реализовать дневной factor-отчет по всем срезам с дельтами          | Medium   | Todo     | Strategy Developer Agent | Есть скрипт/формат отчета, который 1р/день строит factor-анализ со срезами и дельтами к предыдущему дню/5m. | Отчет может сохраняться в файл (txt/docx), а в TG уходят ссылки/выжимка. |
| B23| Monitoring / Alerts   | Интегрировать factor-отчет с `/report_factors` и TG                  | Medium   | Todo     | Strategy Developer Agent | Команда /report_factors в TG включает ссылку или краткий summary большого factor-отчета.                    | Не захламлять TG, основной объем хранить в файле/Notion. |
| B24| Monitoring / Alerts   | Добавить в отчеты оценку рыночного режима на основе funding rate     | Medium   | Todo     | Strategy Developer Agent | В ежедневных отчетах есть явный блок Market Regime (funding-based), значения рассчитываются консистентно.    | Можно использовать агрегированный funding по рынку/топ-инструментам. |
| B25| Risk Guard            | Спроектировать adaptive risk scaling после стабилизации guard        | Medium   | Todo     | Production Safety Agent | Описаны правила (ACTIVE/LOW HEAT/WATCH/DISABLED и др.) и коэффициенты риска; есть тестовый сценарий.        | Внедряется только после нескольких недель стабильной работы risk guard. |
| B26| Dataset / Labeling    | Настроить единый набор индикаторов для LIVE/PAPER/LAB                | High     | Todo     | Strategy Developer Agent | Наборы полей в events_v3 и других таблицах согласованы между режимами; различия задокументированы.          | База для сопоставимых factor-анализов и лаборатории. |
| B27| Dataset / Labeling    | Добавить внешние данные (Coinglass: funding, ликвидации, OI) в лабораторию | Medium | Todo     | Strategy Developer Agent | Лабораторный модуль может подтягивать исторические funding/liq/OI и сохранять их в согласованном формате.    | Важно для анализа режимов и новых фильтров. |
| B28| Validation / Backtest | Реализовать трекинг по одной монете с полным набором индикаторов     | Medium   | Todo     | Strategy Developer Agent | Есть сценарий/скрипт, который для одной монеты показывает в реальном времени все ключевые индикаторы и дельты.| Нужен как "микроскоп" для отладки сетапов. |
| B29| ML Readiness          | Ввести правило: ML для подрежимов только при ≥N (например, 150) сделок | Medium | Todo     | Project Manager Agent   | В документации зафиксирован порог выборки для запуска ML по конкретному режиму; учтен в roadmap/backlog.    | Снижает риск переобучения на шуме. |
| B30| ML Readiness          | Спроектировать систему AI-генерации стратегий/фильтров               | Low      | Todo     | Strategy Developer Agent | Описана архитектура сервиса/агента, который анализирует 200–500 сделок и предлагает новые фильтры/режимы.    | Первая версия может быть оффлайн-инструментом/скриптом. |
| B31| Validation / Backtest | Описать крупноблочный процесс добавления новых стратегий             | Medium   | Todo     | Project Manager Agent   | Есть документ с этапами: исторический прогон → paper → включение в live + критерии перехода между этапами.  | Вяжется с workflow агентов и prompts для AI-инструментов. |
| B32| Monitoring / Alerts   | Добавить контроль дублей outcomes в компакт-отчетах                   | Medium   | Todo     | QA / Validation Agent   | В компакт-отчете появляется метрика по дублям outcomes; при аномалиях отчет подсвечивает проблему.          | Использовать существующие DIAG-скрипты как основу. |
| B33| Monitoring / Alerts   | Реализовать live-дашборд по ключевым метрикам и последним сделкам     | Medium   | Todo     | Strategy Developer Agent | Есть дашборд (Notion/Superset/BI) с текущими показателями и примерами последних сделок с полным контекстом. | Потребует промежуточного слоя: БД/агрегирующие вьюхи поверх CSV. |
| B34| Dataset / Labeling    | Спроектировать переход от CSV к БД для основных таблиц                | Medium   | Todo     | Strategy Developer Agent | Схема БД и связи описаны, есть план миграции с минимальным влиянием на текущие скрипты.                      | Основа для BI и масштабирования. |
| B35| Documentation         | Описать модули системы и архитектурные правила                        | Medium   | Todo     | Project Manager Agent   | Есть краткий, но полный обзор модулей (сбор индикаторов, автоторговля, отчеты, лаборатория) и их связей.     | Можно расширить существующие `PROJECT_STRUCTURE.md`/`ARCHITECTURE_BRIEF.md`. |
| B36| Documentation         | Описать ENV и параметры `.service`, необходимые для настройки         | Medium   | Todo     | Production Safety Agent | Есть документ с перечнем ENV/.service-параметров, назначением и допустимыми значениями.                     | Упростит деплой и передачу проекта. |
| B37| Monitoring / Alerts   | Разобраться, почему в compact-отчете некоторые подрежимы показывают 0 сделок | Critical | Todo     | QA / Validation Agent   | Найдена и задокументирована причина нулевых значений по подрежимам; подтверждено, что фактическое число сделок не теряется; при необходимости исправлены загрузка/агрегация и добавлен тест/диагностика. | Проверить логи, исходные datasets и код compact-отчета, сравнить с историческими периодами, когда сделки были. |
| B38| Dataset / Labeling    | Проверить, что все индикаторы с биржи корректно пишутся в логи и datasets | High     | Todo     | QA / Validation Agent   | Составлен полный список индикаторов, забираемых с биржи; для каждого подтверждено, что он либо логируется и попадает в events/trades/outcomes/lab-данные, либо явно задокументирован как неиспользуемый; при необходимости исправлена логика записи и добавлены проверки. | Нужен проход по коду сбора данных и фактическим CSV/логам для LIVE/PAPER/LAB, чтобы исключить "молчаливое" выпадение полей. |
| B39| Monitoring / Alerts   | Переделать compact-отчет: генерировать TXT и отправлять в TG как файл | High     | Todo     | Strategy Developer Agent | Compact-отчет формируется в `.txt` (читаемый формат), сохраняется как артефакт и отправляется в Telegram как документ; сообщение в TG не перегружено, а файл содержит полный текст. | Обновить `/report` или соответствующую команду: текст в файл + attach; добавить smoke на генерацию файла. |
| B40| Monitoring / Alerts   | Разобраться, почему новый сигнал из TG не попал в compact-отчет | Critical | Todo     | QA / Validation Agent   | Для конкретного периода и сигналов (например `eid=20260318`, стратегия `short_after_pump`/`short_pump`, symbols из TG) подтверждено: (а) сигнал/сделка/исход реально присутствуют в первичных таблицах (events/trades/outcomes), (б) компакт-агрегация их учитывает/не учитывает и на каком шаге происходит выпадение, (в) найденная причина задокументирована и предложено исправление с регресс-тестом/диагностикой. | Сравнить counts: TG сигнал → events/trades/outcomes → агрегаты compact-отчета. |
| B41| Outcome Tracking      | Проверить, почему по кейсам не пришли outcome сообщения в TG (live и paper) | Critical | Todo     | Production Safety Agent | Для кейсов из TG (ENTRY_OK/SHORT/GUARD_BLOCKED_PAPER; ожидаемые исходы) подтверждено: (а) outcome записи создаются (outcomes_v3/эквивалент) и имеют корректные связи event↔trade↔outcome, (б) TG-уведомления об исходе либо не триггерятся из-за правила/фильтра/дедупа, либо триггерятся, но не доходят; (в) результат анализа оформлен в report с таймлайном; (г) после фикса все outcome сообщения приходят для обоих режимов (LIVE и PAPER) как по ТЗ. | Проверить правила уведомлений, rate-limits, дедуп/идемпотентность и обработчики ошибок в outcome-worker. |

